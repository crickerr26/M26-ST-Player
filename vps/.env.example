# Copy to vps/.env and fill in.
# Domain (or subdomain) with an A record pointing at this server's IP.
DOMAIN=stream.example.com
# Optional shared secret for the transcoder's admin routes. Leave blank to disable.
ACCESS_TOKEN=
